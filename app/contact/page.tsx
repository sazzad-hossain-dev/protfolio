import Contact from "@/components/Contact";
import React from "react";

const page = () => {
    return (
        <div className="min-h-screen">
            <Contact />
        </div>
    );
};

export default page;
